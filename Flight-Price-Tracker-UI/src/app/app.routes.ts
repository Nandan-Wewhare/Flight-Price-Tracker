import { Routes } from '@angular/router';
import { FlightTrackerComponent } from './flight-tracker/flight-tracker';

export const routes: Routes = [
    {
        path: '', component: FlightTrackerComponent,
    }
];
